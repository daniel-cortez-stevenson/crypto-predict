language: python
python:
  - "3.5"

install: "touch .env; make create_environment; source activate crypto-predict; make requirments"

script: "pytest ./crypr/tests/unit_test_*.py"

branches:
  only:
  - master
  - develop
